<%= render "shared/auth_navbar" %>

<section class="edit-account-section">
<h2>Complete Registration</h2>

<%= form_for(model: , url: complete_profile_url, html: { method: :put }) do |f| %>
  <!-- Add the bio field -->
  <div class="mb-3">
    <%= f.label :bio, class: "form-label" %>
    <%= f.text_area :bio, class: "form-control", rows: 7 %>
  </div>

  <div class="mb-3">
    <%= f.submit "Update", class: "btn btn-primary" %>
  </div>
<% end %>

<h3>Delete my account</h3>

<%= button_to "Delete my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete %>
<br>
<%= link_to "Back", user_profile_path %>
</section>

<%= render "layouts/footer" %>