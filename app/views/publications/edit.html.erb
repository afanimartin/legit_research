<%= form_for @publication, url: publication_path(@publication), method: :patch do |f| %>
  <% if @publication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publication.errors.count, "error") %> prohibited this publication from being saved:</h2>

      <ul>
      <% @publication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, id: :publication_title %>
  </div>

  <div class="field">
    <%= f.label :abstract %><br>
    <%= f.text_area :abstract, id: :publication_abstract %>
  </div>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, id: :publication_content %>
  </div>

  <div class="actions">
    <%= f.submit "Update Publication" %>
  </div>
<% end %>

<%= link_to 'Back', publications_path %>
